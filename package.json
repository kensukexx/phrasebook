{
  "name": "phrasebook-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Automated tests for ツウジル (index.html is the actual app - this is test tooling only, not a build step)",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:data": "node tests/data-integrity.test.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
